<?xml version="1.0" encoding="utf-8"?>

<!-- Add this style in your res/values/styles.xml file -->
<resources>

    <!-- Modify the style to have a backgroundTint attribute for color -->
    <style name="ResultButtonStyle">
        <item name="android:layout_width">350dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:backgroundTint">#D9D9D9</item> <!-- Set your desired background color -->
        <item name="android:radius">8dp</item>
        <item name="android:elevation">4dp</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- ... -->
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
